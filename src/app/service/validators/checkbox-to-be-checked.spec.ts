import { CheckboxToBeChecked } from './checkbox-to-be-checked';

describe('CheckboxToBeChecked', () => {
  it('should create an instance', () => {
    expect(new CheckboxToBeChecked()).toBeTruthy();
  });
});
